export interface Position {
  uuid: string;
  createdAt: string;
  navigation: string;
  closedAt: string;
  command: string;
  latitude: string;
  longitude: string;
  altitude: string;
  // move: string;
  // speed: string;
  // zoom: string;
}
