export interface Position {
  uuid: string;
  createdAt: string;
  navigation: string;
  updatedAt: string;
  command: string;
  latitude: string;
  longitude: string;
  altitude: string;
  status: string;
  // move: string;
  // speed: string;
  // zoom: string;
}
